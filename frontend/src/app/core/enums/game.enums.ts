export enum GameStatus {
  WAITING = 'WAITING',
  PLAYING = 'PLAYING',
  WON = 'WON',
  LOST = 'LOST'
}

export enum RoomAction {
  JOIN = 'JOIN',
  LEAVE = 'LEAVE',
  KICK = 'KICK',
  DELETE = 'DELETE'
}
